# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
	${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "xen-domd-gfx"
BBFILE_PATTERN_xen-domd-gfx = "^${LAYERDIR}/"
BBFILE_PRIORITY_xen-domd-gfx = "20"

LAYERSERIES_COMPAT_xen-domd-gfx = "kirkstone scarthgap"

